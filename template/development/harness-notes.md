# Local harness notes

> Project-local notes about this repo's agent harness live here — anything
> true of *this* project that the template cannot know: quirks of the verify
> gate on particular machines or CI runners, extra conventions for using the
> slash commands, tool caveats discovered in practice. This file is yours:
> it is generated once and `copier update` never touches it. Its siblings
> [`harness-usage.md`](harness-usage.md) and
> [`tool-bootstrap.md`](tool-bootstrap.md) are template-owned and refreshed
> on every update — local notes added there will collide with the template's
> changes, so keep them here instead.

_Fill in: project-local harness notes, or leave the scaffold until you have
some._
