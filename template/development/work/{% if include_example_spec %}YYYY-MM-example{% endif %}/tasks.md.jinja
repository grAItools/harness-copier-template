# Tasks
{%- from '_macros.jinja' import cmd with context %}

- [ ] Replace this list with checkboxes derived from `plan.md`.
- [ ] Tick boxes as work progresses.
- [ ] When all phases pass `{{ cmd('verify') }}`, archive `scratch.md` and merge.
