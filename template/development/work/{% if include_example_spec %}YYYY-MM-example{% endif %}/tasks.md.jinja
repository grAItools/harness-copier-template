# Tasks
{%- from '_macros.jinja' import cmd with context %}

> Checkbox mirror of `plan.md`, grouped by phase; the Developer ticks each
> box in the same commit as the change it describes. When every box is
> ticked and `{{ cmd('verify') }}` is green, run `/verify` — the feature
> merges only on GO. Before completion, promote anything durable out of
> `scratch.md`; it dies with the feature.

## Phase 1 — `<name>`

- [ ] `<Step from plan.md>`
- [ ] `<Step from plan.md>`
